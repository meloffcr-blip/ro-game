/**
 * ════════════════════════════════════════════════
 *  config.js — إعدادات التطبيق
 *  عدّل هنا فقط، لا تلمس الملفات التانية
 * ════════════════════════════════════════════════
 */

window.APP_CONFIG = {

  // ─── إعدادات لعبة أمسكني ───
  catchGame: {
    duration:  15,   // مدة اللعبة بالثانية
    moveSpeed: 900,  // وقت الانتظار بين كل حركة (ms)
  },

  // ─── إعدادات صائد القلوب ───
  heartCatcher: {
    totalTime: 60,   // مدة اللعبة بالثانية
  },

  // ─── إعدادات الكويز ───
  quiz: {
    questionsCount:  15,
    timePerQuestion: null, // null = بدون وقت محدد
  },

};